1. SELECT USR.USER_VALID_DT,
       USR.USER_STS,
       USR.PASS_FORWARD,
       USR.PASS_REVERSE,
       USR.AUTHORIZED_BY,
       USR.PASS_FINAL,
       CASE WHEN USR.PATH_STS IS NULL THEN '0'
            ELSE USR.PATH_STS
       END PATH_STS,
       USR.USER_STS,
       USR.LOG_MACHINE,
       SUSPEND_PERIOD,
       SUSPEND_PERIOD_TYPE,
       LAST_LOGIN,
       USR.UNSUCCESS_LOGINS,
       SYSDATE systemDate,
       USR.USER_ID,
       USR.FIRST_NAME,
       USR.LAST_NAME,
       (SELECT PTH_CTRL.ENC_TYPE
          FROM PTH_CTRL) ENC_TYPE,
       CASE WHEN USR.PREFERED_LANGUAGE IS NULL
              OR USR.PREFERED_LANGUAGE = '' THEN 'L'
            ELSE USR.PREFERED_LANGUAGE
       END PREFERED_LANGUAGE,
       USR.TODO_REFRESH_TIME,
       USR.ALERT_PER_ONCE,
       USR.CIF_NO,
       USR.ACCESS_RESTRICTED,
       CASE WHEN USR.PASSWD_CHG_FRQ IS NULL THEN 'N'
            ELSE USR.PASSWD_CHG_FRQ
       END PASSWD_CHG_FRQ,
       USR.PASSWD_CHG_DT,
       USR.FREQ_NBR,
       USR.EMPLOYEE_ID,
       USR.EMP_COMP_CODE,
       USR.EMP_BRANCH_CODE,
       USR.IDLE,
       DISABLE_PRINTSCR,
       DEFAULT_PRINTER,
       PASSWD_SET_BY_ADMIN_YN,
       USR.AD_USER_ID,
       USR.AD_USER_NAME,
       USR.AD_USER_DOMAIN,
       USR.USR_PWD_EXPIRY_NOTIFY_DAYS_NO,
       TOKEN_VERIFICATION_YN,
       SHOW_PRINT_PREVIEW_YN,
       PHYSICAL_USR_YN,
       USR.ENABLE_REPORT_ERROR_YN,
       USR.MODIFIED_DATE,
       USR.FIRST_UNSUCCESS_LOGIN_DATE,
       USR.SUSPEND_DATE,
       SUSPEND_BY,
       SEND_OTP_TOKEN_THROUGH,
       EMAIL_ID,
       MOBILE_PHONE,
       LOG_DIFF_PCS,
       MACHINE_ID,
       MACHINE_NAME,
       USR.NOTIFY_USR_BFR_TIMEOUT_MINS,
       IDLE_CHG
  FROM USR
 WHERE USR.USER_ID = ?

parameters:

  ['TL003968']

2. SELECT USR.USER_VALID_DT,
       USR.USER_STS,
       USR.PASS_FORWARD,
       USR.PASS_REVERSE,
       USR.AUTHORIZED_BY,
       USR.PASS_FINAL,
       CASE WHEN USR.PATH_STS IS NULL THEN '0'
            ELSE USR.PATH_STS
       END PATH_STS,
       USR.USER_STS,
       USR.LOG_MACHINE,
       SUSPEND_PERIOD,
       SUSPEND_PERIOD_TYPE,
       LAST_LOGIN,
       USR.UNSUCCESS_LOGINS,
       SYSDATE systemDate,
       USR.USER_ID,
       USR.FIRST_NAME,
       USR.LAST_NAME,
       (SELECT PTH_CTRL.ENC_TYPE
          FROM PTH_CTRL) ENC_TYPE,
       CASE WHEN USR.PREFERED_LANGUAGE IS NULL
              OR USR.PREFERED_LANGUAGE = '' THEN 'L'
            ELSE USR.PREFERED_LANGUAGE
       END PREFERED_LANGUAGE,
       USR.TODO_REFRESH_TIME,
       USR.ALERT_PER_ONCE,
       USR.CIF_NO,
       USR.ACCESS_RESTRICTED,
       CASE WHEN USR.PASSWD_CHG_FRQ IS NULL THEN 'N'
            ELSE USR.PASSWD_CHG_FRQ
       END PASSWD_CHG_FRQ,
       USR.PASSWD_CHG_DT,
       USR.FREQ_NBR,
       USR.EMPLOYEE_ID,
       USR.EMP_COMP_CODE,
       USR.EMP_BRANCH_CODE,
       USR.IDLE,
       DISABLE_PRINTSCR,
       DEFAULT_PRINTER,
       PASSWD_SET_BY_ADMIN_YN,
       USR.AD_USER_ID,
       USR.AD_USER_NAME,
       USR.AD_USER_DOMAIN,
       USR.USR_PWD_EXPIRY_NOTIFY_DAYS_NO,
       TOKEN_VERIFICATION_YN,
       SHOW_PRINT_PREVIEW_YN,
       PHYSICAL_USR_YN,
       USR.ENABLE_REPORT_ERROR_YN,
       USR.MODIFIED_DATE,
       USR.FIRST_UNSUCCESS_LOGIN_DATE,
       USR.SUSPEND_DATE,
       SUSPEND_BY,
       SEND_OTP_TOKEN_THROUGH,
       EMAIL_ID,
       MOBILE_PHONE,
       LOG_DIFF_PCS,
       MACHINE_ID,
       MACHINE_NAME,
       USR.NOTIFY_USR_BFR_TIMEOUT_MINS,
       IDLE_CHG
  FROM USR
 WHERE USR.USER_ID = ?

parameters:

  ['TL003968']


3 .   SELECT SYSDATE FROM DUAL => 1 row

4.    INSERT INTO COMMON_PWS_REQUEST_LOG ( REQUEST_ID, CORE_REQUEST_TIMESTAMP, REQUEST_CONTENT     ,USER_ID     ,HASH_KEY       ,CHANNEL_ID     ,COMPANY_CODE     ,BRANCH_CODE     ,SERVICE_ID     ,BUSINESS_AREA     ,BUSINESS_DOMAIN     ,SERVICE_DOMAIN     ,OPERATION_NAME     ,VERSION                   ,ADDITIONAL_REF           ) SELECT ?, ?, ?     ,?     ,?       ,?     ,?     ,?     ,?     ,?     ,?     ,?     ,?     ,?                   ,?           FROM DUAL ['8c0a18c7-1528-4669-a353-c5d3643c4273', 2026-01-27 08:59:27.283, {stream:StringReader}, 'TL003968', '1N', '1', 1, 304, '4703', 'Retail', 'Products', 'StatementOfAccount', 'returnMiniStatement', '1.0', '2072343601']

5.   {CALL P_RET_API_LAST_N_TRX_AMT( ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?,   ?,   ?, ?, ?, ? ) } [10, 1, NULL, NULL, NULL, NULL, NULL, '2072343601', NULL, NULL, NULL, NULL, 0, NULL, NULL, NULL]

6.   { CALL P_RET_GET_AVLBAL_BR( ?, ?, ?, ?, ?, ?, ?, ?, ? )} [1, 1, 304, 1, 200500, 20723436, 0, '0']

7. SELECT COMP_CODE, BASE_CURRENCY FROM COMPANIES where COMP_CODE = ? [1]

8. DELETE FROM   SYS_RUNTIME_PROC_ARG_TMP   WHERE PROC_NAME = ? AND ARG_NAME = ?  ;     end; ['P_RET_API_LAST_N_TRX_AMT', 'EXCLUDE_REVERSE']
9. INSERT INTO   SYS_RUNTIME_PROC_ARG_TMP    ( PROC_NAME,   ARG_NAME )   values ( ?,   ? )   ;     end; ['P_RET_API_LAST_N_TRX_AMT', 'EXCLUDE_REVERSE']

10. SELECT AMF.COMP_CODE,
       AMF.BRANCH_CODE,
       AMF.CURRENCY_CODE,
       AMF.GL_CODE,
       AMF.CIF_SUB_NO,
       AMF.SL_NO,
       AMF.ADDITIONAL_REFERENCE,
       AMF.IBAN_ACC_NO
  FROM AMF
 WHERE AMF.COMP_CODE = ?
   AND AMF.ADDITIONAL_REFERENCE = ?

parameters:

  [1, '2072343601']

11. SELECT COMP_CODE,
       BRANCH_CODE,
       CODE,
       USER_ID,
       STATUS
  FROM CTSTELLER
 WHERE COMP_CODE = ?
   AND BRANCH_CODE = ?
   AND USER_ID = ?
   AND STATUS <> 'D'

parameters:

  [1, 304, 'TL003968']





